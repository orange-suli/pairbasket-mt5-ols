# Contributing / 研究协作约定

本项目当前处于内部研究与验证阶段。建议任何改动遵循以下规则：

1. **每次只改变一个研究问题。** 不要同时改 Spread、开仓、加仓、退出和仓位配比后再比较结果。
2. **保留可复现性。** 每次优化记录源码版本、测试日期、券商/服务器、测试模型、初始资金、参数范围和 CSV。
3. **不要覆盖原始结果。** 新结果放到新的文件名或子目录。
4. **严格区分开发期与 OOS。** 看过并用于选参的年份不能再标注为真正样本外。
5. **风险指标优先。** 除净利润外必须提交 Sharpe、PF、MaxDD、Worst Basket、最长/平均持仓、活动 Basket 年龄。
6. **参数口径变化必须写入 CHANGELOG。** 尤其是 MaxAbsZ、MaxAdd、账户风险上限等会改变最大风险暴露的参数。
7. **不要提交券商账号、登录凭证、服务器密码、个人路径、终端缓存或含敏感信息的日志。**

建议 PR 描述至少写清：研究假设、代码改动、回测区间、对照组、主要结果、风险变化和是否改变下一步结论。
